eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka
spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-center
      # 加载配置中心目录下的资源文件rocketmq-test.properties
      name: rocketmq
      profile: test