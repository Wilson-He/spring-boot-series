eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka
spring:
  cloud:
    config:
      profile: test
      discovery:
        enabled: true
        service-id: config-center
#     资源文件名
      name: rocketmq