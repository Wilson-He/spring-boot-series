spring:
  cloud:
    config:
      discovery:
        service-id: config-server
      server:
        git:
          uri: https://gitee.com/Wilson-He/spring-boot-series.git
          username: Wilson-He
          basedir: /config-server-properties
          force-pull: true
          password:
      name: rocketmq
      profile: ${spring.profiles.active}
#eureka:
#  client:
#    service-url:
#      defaultZone: http://localhost:8000